# Help Center дані

`src/data/helpArticles.js` експортує три константи:

- `helpCenterCollections` — 8 колекцій з масивами статей
- `allHelpArticles` — плаский масив усіх 437 статей
- `helpArticlesByUrl` — map `url → article` для швидкого lookup в `TemplatePanel`
